name: Eval OpenAI Integration
on: [push]

jobs:
  eval:
    runs-on: ubuntu-latest
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - run: pip install openai
      - run: python components/openai-saas/eval.py
