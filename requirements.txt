# ===========================================================================
# Myntra AI-SPM Test Repo — Root requirements.txt
# This file intentionally contains ALL AI library signals so the scanner
# can detect every asset type from a single repository.
# Each subdirectory also has its own focused requirements.txt.
# ===========================================================================

# ── MCP Server/Client ─────────────────────────────────────────────────────────
mcp>=1.0.0                        # Python MCP SDK (MCP Server signal)

# ── AI Agent Frameworks ───────────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0
langchain-community>=0.2.0
langgraph>=0.1.0
langserve>=0.0.40
google-adk>=0.1.0                 # Google Agent Development Kit
crewai>=0.28.0
crewai-tools>=0.1.0
autogen-agentchat>=0.2.0
pydantic-ai>=0.0.9
dspy-ai>=2.4.0

# ── 1st Party Model Training ─────────────────────────────────────────────────
torch>=2.3.0
torchvision>=0.18.0
transformers>=4.44.0
datasets>=2.20.0
accelerate>=0.33.0
peft>=0.12.0
trl>=0.9.0
scikit-learn>=1.5.0
xgboost>=2.0.0

# ── 3rd Party Model SaaS APIs ────────────────────────────────────────────────
openai>=1.40.0
anthropic>=0.34.0
cohere>=5.6.0
google-generativeai>=0.7.0
mistralai>=0.4.0
groq>=0.9.0

# ── 3rd Party Model Hosted (self-hosted inference) ───────────────────────────
llama-cpp-python>=0.2.90
ollama>=0.3.0

# ── AI Pipeline Orchestration ─────────────────────────────────────────────────
prefect>=2.19.0

# ── Supporting ────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.9.0
python-dotenv>=1.0.0
rich>=13.0.0
